# Footy Predictions

Веб-приложение для футбольных прогнозов с ролями **админ / участники**.

## Стек
- Next.js (App Router)
- Supabase (Postgres + Auth)
- TypeScript
- GitHub

## Запуск локально

```bash
npm install
npm run dev
Переменные окружения (.env.local)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=


⚠️ Значения хранятся только локально, в репозиторий не коммитятся.

Авторизация

Логины без email

Первый пароль по умолчанию: 12345

После первого входа пользователь обязан сменить пароль

Сессия не сохраняется при закрытии сайта

Роли

ADMIN — админ-панель

участники — просмотр таблицы + ввод прогнозов

Основная логика

Этап → Туры → Матчи

В этапе строго 56 матчей

Только админ создаёт/редактирует структуру

Пользователи вводят прогнозы до дедлайна

Очки начисляются после ввода результата

---

# 🧭 `ROADMAP.md`

```md
# Roadmap проекта Footy Predictions

## ✔ Реализовано

### Авторизация
- Логины без email
- Пароль по умолчанию 12345
- Принудительная смена пароля
- Caps Lock warning
- Enter для входа и смены пароля
- Сессия не сохраняется при закрытии сайта

### Админ
- Управление этапами (draft / published / locked)
- Ручной выбор текущего этапа
- Туры и матчи внутри этапа
- Автонумерация матчей в этапе (1–56)
- Назначение команд без повторов в туре
- Результаты матчей
- Начисление очков
- Текущая таблица (иерархия этап → тур → матч → прогнозы)

### Пользователь
- Одна таблица “как у админа”
- Видит все прогнозы и результаты
- Видит очки за матч и сумму по этапу
- Подсветка матчей без прогноза
- Быстрый ввод прогнозов без перезагрузки
- Мягкая подсветка после сохранения

### UX
- Минимум переходов
- Нет лишних разделов (лидерборд, аудит удалены)
- Центрирование значений в ячейках

---

## 🔜 Планируемые улучшения

- Автопереход фокуса на следующий матч без прогноза
- Фильтр: “показать только мои пустые”
- Архив завершённых этапов
- Экспорт таблицы в Excel / PDF
- Мобильная оптимизация
